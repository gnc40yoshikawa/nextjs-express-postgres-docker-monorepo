#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# 修正ファイルのみをESLintでコードチェック Prettierでフォーマットチェック
npx lint-staged

echo "✅ Pre-commit checks passed!"